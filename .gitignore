# Dependencies
node_modules/
# The pi peer deps are linked from the global install, so the lockfile encodes
# this machine's paths and is not portable.
package-lock.json

# Generated output and caches
dist/
build/
coverage/
.cache/
*.tsbuildinfo

# Local runtime files
*.log
*.tmp

# Local environment files
.env
.env.*
!.env.example

# OS metadata
.DS_Store
